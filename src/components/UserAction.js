import React, { Children } from 'react';
import { } from 'react-native';
import { View } from 'react-native-web';

export const UserActionBlock = props => {
	return (
		<View>
			{Children}
		</View>
	)
}